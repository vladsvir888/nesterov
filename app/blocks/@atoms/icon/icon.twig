<svg
  {% if icon.sizes.width %}width="{{ icon.sizes.width }}"{% endif %}
  {% if icon.sizes.height %}height="{{ icon.sizes.height }}"{% endif %}
  class="icon {% if icon.class %}{{ icon.class }}{% endif %}"
>
  <use xlink:href="./assets/sprites/{{ icon.name }}"></use>
</svg>
