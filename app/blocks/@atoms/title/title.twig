{% set level = title.level|default("1") %}

<h{{ level }} {% if title.id %}id="{{ title.id }}"{% endif %} class="title {% if title.class %}{{ title.class }}{% endif %}">
  {{ title.text }}
</h{{ level }}>
